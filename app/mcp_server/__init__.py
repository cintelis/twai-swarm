"""Sprint 14c — MCP server exposing `repo_indexer`'s graph as resources + tools.

See `app/mcp_server/__main__.py` for the entry point and
`_agent-instructions/twai-swarm/repo-mcp-server.md` for the user-facing
catalog. Read-only, stdio transport, single-tenant per process.
"""
