[pytest]
# Auto mode so @pytest.mark.asyncio isn't strictly required, but we keep the
# explicit marker on the async tests anyway for clarity.
asyncio_mode = auto
testpaths = tests
# Clean, quiet output. -ra shows short summaries for skipped/failed/etc.
addopts = -q -ra
