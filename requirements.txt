temporalio==1.8.0
# anthropic>=0.40 handles httpx 0.28's `proxies` → `mounts` rename.
# openai==1.54.3 does NOT — it still calls httpx.AsyncClient(proxies=...)
# which crashes on httpx 0.28+. Hence the httpx pin below. Remove the pin
# when bumping openai to >=1.55.
anthropic>=0.40,<2.0
openai==1.54.3
fastapi==0.115.0
uvicorn[standard]==0.32.0
asyncpg==0.30.0
pydantic==2.9.2
python-dotenv==1.0.1
aiohttp==3.10.11
slowapi==0.1.9
httpx<0.28
